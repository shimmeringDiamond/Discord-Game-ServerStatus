name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Log in to Docker Hub
      run: echo "${{ secrets.DOCKER_HUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
    - name: Push Docker image
      run: |
        TIMESTAMP=$(date +%s)
        docker build . --file Dockerfile --tag discord-minecraft-bot:$TIMESTAMP
        docker tag discord-minecraft-bot:$TIMESTAMP discord-minecraft-bot:latest
        docker push discord-minecraft-bot:$TIMESTAMP
        docker push discord-minecraft-bot:latest

